# Jarvis — Claude Code Guide

사내 업무 시스템 + LLM 컴파일 위키 통합 (Karpathy LLM Wiki 방식). Next.js 15 모노레포(`apps/web`, `apps/worker`, `packages/*`).

## 하네스: Jarvis Feature Development

**목표:** Jarvis 하네스는 **도메인 지식과 방향성만** 담당하고, 개발 방법론(계획·TDD·실행·리뷰·완료 검증·디버깅·병렬 디스패치·브랜치 마감)은 **superpowers 플러그인에 위임**한다. 얇은 진입점(`jarvis-feature`) + 도메인 레퍼런스 스킬 4개.

**트리거:** Jarvis 프로젝트에서 기능 추가·수정·버그 수정·리팩토링·스키마 변경·번역 추가·권한 변경 등 구현 작업을 요청받으면 `jarvis-feature` 스킬을 사용하라. 단일 파일 1~2줄 수정이나 질문은 직접 응답해도 된다.

**구성 요소:**
- 에이전트: 없음 (방법론은 superpowers 서브에이전트 템플릿 사용)
- 스킬: `.claude/skills/jarvis-feature/` (얇은 진입점 오케스트레이터), `.claude/skills/jarvis-architecture/` (모노레포·영향도 체크리스트·파일 변경 순서·검증 게이트), `.claude/skills/jarvis-db-patterns/` (31 스키마·34 권한·sensitivity·경계면 교차 비교), `.claude/skills/jarvis-i18n/` (ko.json·보간 변수), `.claude/skills/jarvis-wiki-feature/` (Karpathy 경계)
- 위임 대상 플러그인: **superpowers** (`brainstorming`, `writing-plans`, `subagent-driven-development`, `executing-plans`, `test-driven-development`, `verification-before-completion`, `requesting-code-review`, `receiving-code-review`, `systematic-debugging`, `dispatching-parallel-agents`, `using-git-worktrees`, `finishing-a-development-branch`)
- 훅: `.claude/settings.json` (PostToolUse → `scripts/check-schema-drift.mjs --hook`, advisory). CI/pre-commit은 동일 스크립트에 `--ci`/`--precommit`을 붙여 블로킹.
- Codex 공유 지시문: `AGENTS.md` (같은 하네스 원칙을 Codex CLI에서도 재사용)
- 공유 스크립트: `scripts/check-schema-drift.mjs` (Claude Code hook / Codex 수동 / CI 공용)

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-10 | 초기 구성 (경량 3인 팀) | 전체 | 사내 업무 시스템 + 사내 위키 통합 프로젝트 경량 하네스 요청 |
| 2026-04-10 | Drizzle schema drift 훅 + Codex용 `AGENTS.md` 추가 | `.claude/settings.json`, `scripts/check-schema-drift.mjs`, `AGENTS.md` | 경량 훅 1(advisory) 설치 + Codex CLI에서도 동일 원칙 따르도록 지시문 미러링 |
| 2026-04-14 | schema-drift hook에 `--ci`/`--precommit` blocking 모드 추가 | `scripts/check-schema-drift.mjs`, `scripts/tests/check-schema-drift.test.mjs` | G5 게이트: 의도적 drift에서 CI exit 1 보장 (Phase-7A PR#4) |
| 2026-04-15 | Karpathy LLM Wiki 피벗 반영 — 자기정의 업데이트 | L3 자기정의 ("RAG AI 포털" → "LLM 컴파일 위키") | CLAUDE.md가 RAG 시대 정체성을 system prompt에 주입하는 drift 수정. Phase-W3 이후 실제 아키텍처 반영 |
| 2026-04-15 | `jarvis-wiki-feature` 스킬 신설 | `.claude/skills/jarvis-wiki-feature/SKILL.md` | Karpathy 피벗 후 위키 도메인(auto/manual 경계, wiki-fs 경유, DB projection only) 전용 레퍼런스 필요 |
| 2026-04-20 | 하네스 drift 수정 — "RAG AI 포털" 용어 잔존 제거 + CLAUDE.md 구성 요소에 `jarvis-wiki-feature` 추가 | `jarvis-planner.md`, `jarvis-architecture/SKILL.md`, `jarvis-feature/SKILL.md`, `CLAUDE.md` | 2026-04-15 자기정의 업데이트가 하위 에이전트·스킬에 전파되지 않았고, 신설 스킬이 구성 요소 목록에 미등록. `/harness:harness` 점검으로 발견 |
| 2026-04-20 | 하네스 전면 재작성 (Karpathy 피벗 이후 아키텍처 현행화) | `jarvis-architecture/SKILL.md` 전면 재작성 · `jarvis-wiki-feature/SKILL.md` 전면 재작성 · `jarvis-db-patterns/SKILL.md` 보강(31 스키마, 34 권한, 5 역할, Ask AI 세션 모델, Wiki projection 무결성, Case 독립 벡터 공간) · `jarvis-i18n/SKILL.md` en.json 각주 · `jarvis-planner.md` 영향도 체크리스트 확장(Ask AI/Wiki-fs/CLIProxyAPI/Audit) · `jarvis-builder.md` 파일 변경 순서 현행화(20단계) + 도메인별 특별 규칙(Wiki/Ask AI/Case) · `jarvis-integrator.md` 자동화 명령 보강(wiki:check, audit:rsc, eval:budget-test, check-schema-drift --precommit) · `jarvis-feature/SKILL.md` 오케스트레이터 자동화 체크리스트 보강 | 3개 서브에이전트 전수 스캔 결과: packages 6→8(wiki-fs/wiki-agent 신설), Ask AI 단일 claim RAG → 6-lane 라우터 + page-first retrieval, ingest 4단계 분해, 스키마 29→31 파일(case/directory/ask-conversation/llm-call-log/additional-development 등), 권한 34개/5역할, Redis 제거, CLIProxyAPI 게이트웨이 — 기존 아키텍처 스킬이 옛 RAG 시대 모델에 머물러 planner 영향도 분석 품질에 직접 타격. 3인 경량 팀 기조는 유지 |
| 2026-04-24 | Ask AI Harness-first 전환 완료 (Phase A–G) — embedding 파이프라인 전면 제거, 4-tool agent + SSE adapter + page-first citation 도입 | `packages/ai/**`, `packages/ai/agent/**`, `packages/wiki-agent/**`, `apps/web/components/ai/AnswerCard.tsx`, `apps/web/e2e/ask-harness.spec.ts`, `AGENTS.md`, `README.md`, `jarvis-architecture SKILL` | PR #16-#22 / #16 plan → #17 Phase A tools → #18 Phase B1/B2 agent loop → #19 Phase C wiki index/log → #20 Phase D+E embedding drop → #21 Phase F BM25 only → #22 Phase B3/B4 ask.ts delegation + `[[slug]]` → Phase G E2E + docs. `route.ts` 미수정(계약 안정), legacy 코드는 `@deprecated` 배너로 1주 burn-in 후 삭제 예정 |
| 2026-04-22 | **방법론을 superpowers 플러그인에 전면 위임 + 3인 에이전트 폐기.** `jarvis-planner.md`·`jarvis-builder.md`·`jarvis-integrator.md` 삭제 · `jarvis-architecture/SKILL.md`에 "영향도 체크리스트(17계층)"·"파일 변경 순서 20단계"·"검증 게이트 명령" 3개 섹션 이관 + description 갱신 · `jarvis-db-patterns/SKILL.md`에 "경계면 교차 비교 체크리스트"(shape/권한/sensitivity/nullable/마이그레이션/i18n) 섹션 신설 + description 갱신 · `jarvis-feature/SKILL.md` 전면 슬림화(~260줄 → ~120줄, superpowers 7단계 워크플로우에 Jarvis 도메인 컨텍스트를 주입하는 얇은 진입점) · `AGENTS.md` 전면 재작성(Codex CLI용 superpowers 설치 안내 포함) · `README.md:245` + `CONTRIBUTING.md §4` stale 참조 동기화 | Decision: 방법론(TDD·테스트·리뷰·계획·검증·디버깅 등)은 superpowers 플러그인에 위임하고, Jarvis 하네스는 도메인/방향만 유지. Codex CLI에서도 superpowers 플러그인 설치 가능. 방법론 중복 유지보수 제거 + 업스트림 혜택 자동 수용. 도메인 지식은 4개 스킬(architecture/db-patterns/i18n/wiki-feature)에 응집. |
| 2026-04-30 | 회사 마스터 그리드 표준 도입 | `apps/web/app/(app)/admin/companies/**`(grid + cells + state hook), `packages/db/schema/company.ts`(컬럼 정정), `packages/db/seed/<companies-source>` (366건 시드, 사내 zip 채널), `.claude/skills/jarvis-architecture/SKILL.md`("그리드 표준 화면" 섹션) | 366건 마스터 데이터 이관 + 향후 데이터 다량 화면(코드/메뉴/인프라 등)의 표준 템플릿 정의. legacy company master export(회사 코드/사업장 구분/대표사 여부 등) 컬럼 매핑 보정 |
| 2026-04-30 | RBAC 메뉴 트리 도입 (DB 기반 menu_item + menu_permission UNION 모델) | `packages/db/schema/menu*.ts`, `packages/db/seed/menus.ts`, `apps/web/lib/server/menu-tree.ts`, `apps/web/components/layout/Sidebar.tsx`, `apps/web/components/layout/CommandPalette.tsx`, `apps/web/components/layout/Topbar.tsx`, `apps/web/components/layout/AppShell.tsx`, `apps/web/components/auth/Authorized.tsx`, `apps/web/app/(app)/layout.tsx`, `apps/web/app/(app)/admin/menus/page.tsx`, `apps/web/lib/routes.ts`(@deprecated) | 사이드바·CommandPalette를 hard-coded routes.ts에서 DB 기반으로 전환 + RBAC 권한별 메뉴 가시성 + 부모 cascade. Authorized HOC로 UI hint 지원. routes.ts는 @deprecated 표시, 추후 제거. |
| 2026-05-01 | RBAC 메뉴 트리 follow-up (Task 6 리뷰 fix + Task 7 E2E + Task 9 하네스 갱신) | `apps/web/components/auth/Authorized.tsx`(JSDoc 정정 + `Permission` 타입 + 6 unit tests), `apps/web/app/(app)/admin/menus/page.tsx`(redirect `/login`→`/dashboard?error=forbidden`), `apps/web/app/(app)/admin/menus/_components/MenuTreeViewer.tsx`(i18n 4개 키 + dev-warn), `apps/web/messages/{ko,en}.json`(Admin.Menus.{empty,childBadge,visible,hidden} + 영문 미러), `apps/web/lib/queries/admin.ts` + `apps/web/lib/server/menu-tree.ts`(getMenuTree ↔ getVisibleMenuTree cross-reference JSDoc), `apps/web/components/admin/MenuEditor.tsx`(orphan 삭제), `apps/web/e2e/sidebar-rbac.spec.ts`(NEW — admin/viewer 메뉴 가시성 + forbidden redirect), `.claude/skills/jarvis-architecture/SKILL.md`("RBAC 메뉴 트리" 섹션 추가) | 4-각도 리뷰(spec/code-quality/architecture/adversarial)에서 발견된 P1/P2 7건 정리. 핵심: login 루프 방지(redirect 정정), 한국어 하드코딩 → i18n, Authorized 보안 경계 명시 + typo 차단, getMenuTree 명명 충돌 해소. |
| 2026-05-01 | ibsheet 행동 패리티 baseline 5종 신설 + jarvis-architecture skill에 매핑 섹션 추가 | `apps/web/components/grid/{utils/makeHiddenSkipCol,DataGridToolbar,CodeGroupPopupLauncher}`, `apps/web/lib/{hooks/useUrlFilters,utils/validateDuplicateKeys}`, `.claude/skills/jarvis-architecture/SKILL.md` | P1.5(컬럼 fix) + P2(신규 라우트)와 평행 진행되는 행동 layer 인프라. 5 GridContainer + DataGrid 본체 미수정으로 충돌 회피. P2-A 세션이 5 화면에 적용. |
| 2026-05-02 | 그리드 표준 화면 섹션 전면 확장 (9개 하위 섹션) | `.claude/skills/jarvis-architecture/SKILL.md` "그리드 표준 화면" 섹션 | 11개 baseline 채택 도메인 전수 적용 후, 기존 3개 항목(컴포넌트/필수 7기능/디자인 토큰)을 9개 항목으로 확장: ① baseline 컴포넌트 위치(공유 vs 도메인) ② 필수 기능 7+1 ③ 디자인 토큰 표 ④ 버튼 표준화(라벨 i18n 키/variant/상태별 텍스트) ⑤ 컬럼 컨벤션(PK→본문→audit readonly) ⑥ Server action 컨벤션(list/save 두 함수+트랜잭션+audit_log) ⑦ 금지 패턴 9건(TanStack Table 신규/카드형/모달폼/raw 색상 등) ⑧ 신규 그리드 PR 체크리스트 11항 ⑨ 참고. Decision: admin/users 머지 후 그리드 룰을 `jarvis-architecture` 한 곳에 응집해 drift 방지. spec-reviewer가 신규 그리드 PR에서 체크리스트로 검증. |
| 2026-05-02 | DatePicker 표준 컴포넌트 도입 + 공휴일관리 그리드 baseline 전환 + native `<input type="date">` 전면 제거 | `apps/web/components/ui/DatePicker/**`, `apps/web/app/(app)/holidays/**`, `apps/web/components/grid/cells/EditableDateCell.tsx`, `.claude/skills/jarvis-architecture/SKILL.md`("표준 입력 컴포넌트" 섹션 신설), `CLAUDE.md` | 한국 IME 환경에서 native date input의 자릿수 자동 분할이 깨져 연도 칸에 6글자가 입력되는 버그 + 토/일/공휴일 시각 표시 요구. 11개 사용처 일괄 sweep. 향후 모든 달력 사용처에서 강제 |
| 2026-05-04 | **CI db:migrate 인프라 수정 — Option D (외부 사용자 흐름) 채택.** PR #103 이후 `packages/db/drizzle/` gitignore 정책에도 불구하고 PR #70/#78/#82/#108/#110 등이 `git add -f`로 0062-0070 SQL + `_journal.json`(idx 0-70 entries)을 강제 커밋 → CI `pnpm db:migrate`가 0000 SQL 부재로 ENOENT, schema-drift/unit-and-integration/e2e-wiki 모두 실패. 해결: ① 강제 추가 19개 파일 `git rm --cached` (drizzle/00{62-70}*.sql + meta/00{62-70}_snapshot.json + meta/_journal.json), ② 3개 워크플로우(schema-drift/test/e2e-wiki)에 "Generate baseline migration from schema" step 추가 → `rm -rf packages/db/drizzle && pnpm --filter @jarvis/db generate --name ci_baseline` (README §17.1 외부 사용자 흐름과 정합), ③ `.local/db/drizzle/`에 0000-0070 archive 복구 (71/71 SQL, 63/71 snapshot — 0037-0044/0046 8개는 git history에 한 번도 없음, 향후 generate에는 LATEST snapshot만 필요해 무영향), ④ `scripts/build-db-private-zip.mjs` 신설 (private zip 채널 §17.2 자동화 — `.local/db/drizzle/` + `packages/db/seed/`를 `packages\db\...` 경로로 zip해 서버 unzip 시 그대로 복원). | `.github/workflows/{schema-drift,test,e2e-wiki}.yml`, `packages/db/drizzle/` (untrack 19), `scripts/build-db-private-zip.mjs` (신설), `.local/db/drizzle/` (운영자 로컬 archive — git 무관) | PR #108-#111이 모두 같은 root cause로 fail. 기능 영향 없음 (schema TS가 SoT, 로컬/운영 DB는 그대로, gitignore는 디스크 파일 안 지움). 외부 GitHub 공개 정책(README §17) 유지 + zip 채널 정착으로 향후 운영 배포 표준화. |
| 2026-05-04 | **Legacy Oracle → Jarvis Postgres ETL 메타 전략 도입.** 3-layer hybrid: Layer 1 = TS bootstrap (`packages/db/seed/*.ts`), Layer 2 = Python→SQL ETL (`.local/legacy-ssms/postgres/<NN>_<module>/`), Layer 3 = `pnpm db:reset` runner (`.local/scripts/db-reset.sh`로 위임 — drop drizzle+public schema → migrate → seed conditional → ETL apply, lex sort). Pure INSERT + deterministic UUID(uuid5) → 멱등 wipe-and-reload. 외부 사용자(`.local/scripts/db-reset.sh` 부재) `pnpm db:reset` 자체가 fail — 사내 사용자만 실행 가능. 운영 zip 채널(`.local/scripts/build-db-private-zip.mjs`)에 ETL SQL + db-reset.sh 포함. 새 모듈은 `cp etl-template.example.py etl-<module>.py` 후 채우기 (helper는 `etl-template.py`). 본 PR은 메타 인프라(template + db:reset + zip 보강 + 문서)만 만들고, 각 모듈 ETL은 follow-up spec/plan/실행. P0 = 70_operations(maintenance/schedule/doc-numbers/faq) 4개. **운영 도구는 모두 `.local/` 하위 (gitignored, zip 채널)**: scripts·spec·plan·HANDOFF 모두 `.local/scripts/` 또는 `.local/docs/`. `packages/db/seed/sales-{plan-perf,stats-codes}.ts`는 `git rm --cached` 정책 정합. **Follow-up:** etl-contractor.py:221이 hardcoded `out_dir = base / "postgres" / "contractor"` — Task 4 verification에서 폴더를 `40_contractor`로 rename했지만 다음 regen 시 `contractor/` 다시 만듬. 단순 수정 별도 PR. | `package.json`(`db:reset` → `bash .local/scripts/db-reset.sh`), `README.md` §17 보강, `CLAUDE.md`, **(신설, gitignored)** `.local/scripts/db-reset.sh` + `.local/scripts/build-db-private-zip.mjs` + `.local/docs/2026-05-04-...md` + `.local/legacy-ssms/scripts/etl-template{,.example}.py` + `.local/legacy-ssms/postgres/40_contractor/` rename | 사용자 발언 "전체 데이터 oracle => postgre인데 새로운 스키마에 옮기는 대작업" + "DB 데이터 git 추적 금지". 단발성 X — 로컬에서 wipe-and-reload 반복 + 운영 동일 파일로 배포. 기존 자산(etl-contractor, sales/postgres, packages/db/seed/menus.ts) 깨지 않음. 운영 도구 자체도 외부 GitHub 노출 금지 → `.local/` 하위. spec/plan은 zip 채널의 `.local/docs/` 참조. |
| 2026-05-04 | **DB seed pipeline 부활 + Phase 2/3 ETL 실행 (메타 전략 후속 — P0 70_operations + 10_admin/user 완료).** Layer 1 (전부 gitignored, 디스크 only) `packages/db/seed/{dev,permissions,menu-permissions,companies-tsmt001,code-groups,parsers/parse-tsmt001,parsers/__tests__/parse-tsmt001.test}.ts` 부활/신설: PERMISSIONS const(47) + ROLE_PERMISSIONS(5 role) → permission/role/role_permission 130 mapping + menu_permission 99 mapping 시드. workspace 3 (jarvis + e2e + _system). dev admin user 1명 (employee_id=ADMIN, **scrypt** password hash — bcrypt 아닌 jarvis-auth 패턴, env override `DEV_ADMIN_PASSWORD`). TMST001 → company 366 + code_group 3 (C10100/C10005/C10002) Layer 1 예외로 유지(TS parser 활용). Layer 2 ETL (gitignored): `etl-admin-user.py` (TSYS305 → user 122 incl 26 contractor, 1 email dedup) + 70_operations 4 ETL (`etl-{maintenance,schedule,document-number,faq}.py` → maintenance_assignment 322 + schedule_event 7 + document_number 502 + faq_entry 13). Findings F2 + F3 해소. `40_contractor/01_user_contractor.sql` 폐기 (TSYS305 ETL로 통합 — `00_sabun_uuid_map.tsv` + `_skip_report.txt` 동시 삭제). `db-reset.sh` 보강 3건: ① psql PATH 미발견 시 `/c/Program Files/PostgreSQL/*/bin` fallback ② `packages/db/drizzle/_journal.json` 부재 시 `pnpm --filter @jarvis/db generate --name ci_baseline` 자동 실행 ③ `WORKSPACE_ID` 기본값 `7a995ab9-525c-415a-a8ea-34863fe002bb` (jarvis) — E2E는 명시 override. `etl-template.py`에 multi-line INSERT 파싱 추가 (TITLE/MEMO/QUESTION에 embedded `\n` 처리, single-line 호환). `pnpm db:reset` 16-table 멱등 검증 통과 (2회 연속 동일 count). `.local/docs/2026-05-04-legacy-etl-meta-strategy-design.md` §6.1/6.2/6.4/6.5/§10 갱신. **Follow-up (별도 PR):** sales/ Oracle REM 주석 정리 + 70_operations FK skip 보완 (legacy SABUN 부재 사용자 처리, 76+55 row). | **(commit)** `CLAUDE.md` 변경 이력만. **(gitignored, 디스크 only — 사내 zip 채널 배포)** `packages/db/seed/{dev,permissions,menu-permissions,companies-tsmt001,code-groups,parsers/parse-tsmt001}.ts` + `parsers/__tests__/parse-tsmt001.test.ts` · `packages/db/seed/menus.ts` (MENU_SEEDS export) · `.local/scripts/db-reset.sh` (3개 보강) · `.local/legacy-ssms/scripts/{etl-template(multi-line),etl-contractor(F2/F3),etl-admin-user(신설),etl-{maintenance,schedule,document-number,faq}(신설)}.py` · `.local/legacy-ssms/postgres/{10_admin/01_user,40_contractor/{02,03}_*(regen),70_operations/{01,02,03,04}_*(신설)}.sql` · `.local/docs/2026-05-04-legacy-etl-meta-strategy-design.md` (§6/§10 갱신) | docker DB가 partial state에 있었음 (permission 29/47, company 732 dup 380, organization/holiday 0, maintenance/schedule/doc-num/faq 테이블 부재). 폐기된 `.local/db/seed/` 부활 + 누락 시드(permission 18 + menu_permission 99) 추가. PERMISSIONS SoT(47개) ↔ DB drift 해소. **사용자 결정:** SABUN 전략 A · TMST001 366 Layer 1 유지 · workspace_id placeholder · TSMT100 SEQ→orderSeq · TSMT050 PROJECT_ID drop (502/502 NULL 확인) · 40_contractor user 폐기 · 데이터 보존 없음. **사용자 발언:** "지금 db seed만들면 이걸로 로컬테스트 쭉 계속하고 다되면 운영서버 db에 넣고, 앞으로 운영 서버db에 붙어서 직접 작업할 예정". 운영 zip 채널이 self-contained (psql PATH + drizzle baseline + WORKSPACE_ID 모두 자동 처리). |
