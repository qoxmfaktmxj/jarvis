# Jarvis — Claude Code Guide

사내 업무 시스템 + LLM 컴파일 위키 통합 (Karpathy LLM Wiki 방식). Next.js 15 모노레포(`apps/web`, `apps/worker`, `packages/*`).

## 하네스: Jarvis Feature Development

**목표:** Jarvis 하네스는 **도메인 지식과 방향성만** 담당하고, 개발 방법론(계획·TDD·실행·리뷰·완료 검증·디버깅·병렬 디스패치·브랜치 마감)은 **superpowers 플러그인에 위임**한다. 얇은 진입점(`jarvis-feature`) + 도메인 레퍼런스 스킬 4개.

**트리거:** Jarvis 프로젝트에서 기능 추가·수정·버그 수정·리팩토링·스키마 변경·번역 추가·권한 변경 등 구현 작업을 요청받으면 `jarvis-feature` 스킬을 사용하라. 단일 파일 1~2줄 수정이나 질문은 직접 응답해도 된다.

**구성 요소:**
- 에이전트: 없음 (방법론은 superpowers 서브에이전트 템플릿 사용)
- 스킬: `.claude/skills/jarvis-feature/` (얇은 진입점 오케스트레이터), `.claude/skills/jarvis-architecture/` (모노레포·영향도 체크리스트·파일 변경 순서·검증 게이트 + `references/grid-standard.md` 그리드 표준), `.claude/skills/jarvis-db-patterns/` (54 스키마·23 권한·4 역할·RBAC + workspaceId·경계면 교차 비교), `.claude/skills/jarvis-i18n/` (ko.json·보간 변수), `.claude/skills/jarvis-wiki-feature/` (Karpathy 경계)
- 위임 대상 플러그인: **superpowers** (`brainstorming`, `writing-plans`, `subagent-driven-development`, `executing-plans`, `test-driven-development`, `verification-before-completion`, `requesting-code-review`, `receiving-code-review`, `systematic-debugging`, `dispatching-parallel-agents`, `using-git-worktrees`, `finishing-a-development-branch`)
- 훅: `.claude/settings.json` (PostToolUse → `scripts/check-schema-drift.mjs --hook`, advisory). CI/pre-commit은 동일 스크립트에 `--ci`/`--precommit`을 붙여 블로킹.
- Codex 공유 지시문: `AGENTS.md` (같은 하네스 원칙을 Codex CLI에서도 재사용)
- 공유 스크립트: `scripts/check-schema-drift.mjs` (Claude Code hook / Codex 수동 / CI 공용)

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-10 | 초기 구성 (경량 3인 팀) | 전체 | 사내 업무 시스템 + 사내 위키 통합 프로젝트 경량 하네스 요청 |
| 2026-04-10 | Drizzle schema drift 훅 + Codex용 `AGENTS.md` 추가 | `.claude/settings.json`, `scripts/check-schema-drift.mjs`, `AGENTS.md` | 경량 훅 1(advisory) 설치 + Codex CLI에서도 동일 원칙 따르도록 지시문 미러링 |
| 2026-04-14 | schema-drift hook에 `--ci`/`--precommit` blocking 모드 추가 | `scripts/check-schema-drift.mjs`, `scripts/tests/check-schema-drift.test.mjs` | G5 게이트: 의도적 drift에서 CI exit 1 보장 (Phase-7A PR#4) |
| 2026-04-15 | Karpathy LLM Wiki 피벗 반영 — 자기정의 업데이트 | L3 자기정의 ("RAG AI 포털" → "LLM 컴파일 위키") | CLAUDE.md가 RAG 시대 정체성을 system prompt에 주입하는 drift 수정. Phase-W3 이후 실제 아키텍처 반영 |
| 2026-04-15 | `jarvis-wiki-feature` 스킬 신설 | `.claude/skills/jarvis-wiki-feature/SKILL.md` | Karpathy 피벗 후 위키 도메인(auto/manual 경계, wiki-fs 경유, DB projection only) 전용 레퍼런스 필요 |
| 2026-04-20 | 하네스 drift 수정 — "RAG AI 포털" 용어 잔존 제거 + CLAUDE.md 구성 요소에 `jarvis-wiki-feature` 추가 | `jarvis-planner.md`, `jarvis-architecture/SKILL.md`, `jarvis-feature/SKILL.md`, `CLAUDE.md` | 2026-04-15 자기정의 업데이트가 하위 에이전트·스킬에 전파되지 않았고, 신설 스킬이 구성 요소 목록에 미등록. `/harness:harness` 점검으로 발견 |
| 2026-04-20 | 하네스 전면 재작성 (Karpathy 피벗 이후 아키텍처 현행화) | `jarvis-architecture/SKILL.md` 전면 재작성 · `jarvis-wiki-feature/SKILL.md` 전면 재작성 · `jarvis-db-patterns/SKILL.md` 보강(31 스키마, 34 권한, 5 역할, Ask AI 세션 모델, Wiki projection 무결성, Case 독립 벡터 공간) · `jarvis-i18n/SKILL.md` en.json 각주 · `jarvis-planner.md` 영향도 체크리스트 확장(Ask AI/Wiki-fs/CLIProxyAPI/Audit) · `jarvis-builder.md` 파일 변경 순서 현행화(20단계) + 도메인별 특별 규칙(Wiki/Ask AI/Case) · `jarvis-integrator.md` 자동화 명령 보강(wiki:check, audit:rsc, eval:budget-test, check-schema-drift --precommit) · `jarvis-feature/SKILL.md` 오케스트레이터 자동화 체크리스트 보강 | 3개 서브에이전트 전수 스캔 결과: packages 6→8(wiki-fs/wiki-agent 신설), Ask AI 단일 claim RAG → 6-lane 라우터 + page-first retrieval, ingest 4단계 분해, 스키마 29→31 파일(case/directory/ask-conversation/llm-call-log/additional-development 등), 권한 34개/5역할, Redis 제거, CLIProxyAPI 게이트웨이 — 기존 아키텍처 스킬이 옛 RAG 시대 모델에 머물러 planner 영향도 분석 품질에 직접 타격. 3인 경량 팀 기조는 유지 |
| 2026-04-24 | Ask AI Harness-first 전환 완료 (Phase A–G) — embedding 파이프라인 전면 제거, 4-tool agent + SSE adapter + page-first citation 도입 | `packages/ai/**`, `packages/ai/agent/**`, `packages/wiki-agent/**`, `apps/web/components/ai/AnswerCard.tsx`, `apps/web/e2e/ask-harness.spec.ts`, `AGENTS.md`, `README.md`, `jarvis-architecture SKILL` | PR #16-#22 / #16 plan → #17 Phase A tools → #18 Phase B1/B2 agent loop → #19 Phase C wiki index/log → #20 Phase D+E embedding drop → #21 Phase F BM25 only → #22 Phase B3/B4 ask.ts delegation + `[[slug]]` → Phase G E2E + docs. `route.ts` 미수정(계약 안정), legacy 코드는 `@deprecated` 배너로 1주 burn-in 후 삭제 예정 |
| 2026-04-22 | **방법론을 superpowers 플러그인에 전면 위임 + 3인 에이전트 폐기.** `jarvis-planner.md`·`jarvis-builder.md`·`jarvis-integrator.md` 삭제 · `jarvis-architecture/SKILL.md`에 "영향도 체크리스트(17계층)"·"파일 변경 순서 20단계"·"검증 게이트 명령" 3개 섹션 이관 + description 갱신 · `jarvis-db-patterns/SKILL.md`에 "경계면 교차 비교 체크리스트"(shape/권한/sensitivity/nullable/마이그레이션/i18n) 섹션 신설 + description 갱신 · `jarvis-feature/SKILL.md` 전면 슬림화(~260줄 → ~120줄, superpowers 7단계 워크플로우에 Jarvis 도메인 컨텍스트를 주입하는 얇은 진입점) · `AGENTS.md` 전면 재작성(Codex CLI용 superpowers 설치 안내 포함) · `README.md:245` + `CONTRIBUTING.md §4` stale 참조 동기화 | Decision: 방법론(TDD·테스트·리뷰·계획·검증·디버깅 등)은 superpowers 플러그인에 위임하고, Jarvis 하네스는 도메인/방향만 유지. Codex CLI에서도 superpowers 플러그인 설치 가능. 방법론 중복 유지보수 제거 + 업스트림 혜택 자동 수용. 도메인 지식은 4개 스킬(architecture/db-patterns/i18n/wiki-feature)에 응집. |
| 2026-04-30 | 회사 마스터 그리드 표준 도입 | `apps/web/app/(app)/admin/companies/**`(grid + cells + state hook), `packages/db/schema/company.ts`(컬럼 정정), `packages/db/seed/<companies-source>` (366건 시드, 사내 zip 채널), `.claude/skills/jarvis-architecture/SKILL.md`("그리드 표준 화면" 섹션) | 366건 마스터 데이터 이관 + 향후 데이터 다량 화면(코드/메뉴/인프라 등)의 표준 템플릿 정의. legacy company master export(회사 코드/사업장 구분/대표사 여부 등) 컬럼 매핑 보정 |
| 2026-04-30 | RBAC 메뉴 트리 도입 (DB 기반 menu_item + menu_permission UNION 모델) | `packages/db/schema/menu*.ts`, `packages/db/seed/menus.ts`, `apps/web/lib/server/menu-tree.ts`, `apps/web/components/layout/Sidebar.tsx`, `apps/web/components/layout/CommandPalette.tsx`, `apps/web/components/layout/Topbar.tsx`, `apps/web/components/layout/AppShell.tsx`, `apps/web/components/auth/Authorized.tsx`, `apps/web/app/(app)/layout.tsx`, `apps/web/app/(app)/admin/menus/page.tsx`, `apps/web/lib/routes.ts`(@deprecated) | 사이드바·CommandPalette를 hard-coded routes.ts에서 DB 기반으로 전환 + RBAC 권한별 메뉴 가시성 + 부모 cascade. Authorized HOC로 UI hint 지원. routes.ts는 @deprecated 표시, 추후 제거. |
| 2026-05-01 | RBAC 메뉴 트리 follow-up (Task 6 리뷰 fix + Task 7 E2E + Task 9 하네스 갱신) | `apps/web/components/auth/Authorized.tsx`(JSDoc 정정 + `Permission` 타입 + 6 unit tests), `apps/web/app/(app)/admin/menus/page.tsx`(redirect `/login`→`/dashboard?error=forbidden`), `apps/web/app/(app)/admin/menus/_components/MenuTreeViewer.tsx`(i18n 4개 키 + dev-warn), `apps/web/messages/{ko,en}.json`(Admin.Menus.{empty,childBadge,visible,hidden} + 영문 미러), `apps/web/lib/queries/admin.ts` + `apps/web/lib/server/menu-tree.ts`(getMenuTree ↔ getVisibleMenuTree cross-reference JSDoc), `apps/web/components/admin/MenuEditor.tsx`(orphan 삭제), `apps/web/e2e/sidebar-rbac.spec.ts`(NEW — admin/viewer 메뉴 가시성 + forbidden redirect), `.claude/skills/jarvis-architecture/SKILL.md`("RBAC 메뉴 트리" 섹션 추가) | 4-각도 리뷰(spec/code-quality/architecture/adversarial)에서 발견된 P1/P2 7건 정리. 핵심: login 루프 방지(redirect 정정), 한국어 하드코딩 → i18n, Authorized 보안 경계 명시 + typo 차단, getMenuTree 명명 충돌 해소. |
| 2026-05-01 | ibsheet 행동 패리티 baseline 5종 신설 + jarvis-architecture skill에 매핑 섹션 추가 | `apps/web/components/grid/{utils/makeHiddenSkipCol,DataGridToolbar,CodeGroupPopupLauncher}`, `apps/web/lib/{hooks/useUrlFilters,utils/validateDuplicateKeys}`, `.claude/skills/jarvis-architecture/SKILL.md` | P1.5(컬럼 fix) + P2(신규 라우트)와 평행 진행되는 행동 layer 인프라. 5 GridContainer + DataGrid 본체 미수정으로 충돌 회피. P2-A 세션이 5 화면에 적용. |
| 2026-05-02 | 그리드 표준 화면 섹션 전면 확장 (9개 하위 섹션) | `.claude/skills/jarvis-architecture/SKILL.md` "그리드 표준 화면" 섹션 | 11개 baseline 채택 도메인 전수 적용 후, 기존 3개 항목(컴포넌트/필수 7기능/디자인 토큰)을 9개 항목으로 확장: ① baseline 컴포넌트 위치(공유 vs 도메인) ② 필수 기능 7+1 ③ 디자인 토큰 표 ④ 버튼 표준화(라벨 i18n 키/variant/상태별 텍스트) ⑤ 컬럼 컨벤션(PK→본문→audit readonly) ⑥ Server action 컨벤션(list/save 두 함수+트랜잭션+audit_log) ⑦ 금지 패턴 9건(TanStack Table 신규/카드형/모달폼/raw 색상 등) ⑧ 신규 그리드 PR 체크리스트 11항 ⑨ 참고. Decision: admin/users 머지 후 그리드 룰을 `jarvis-architecture` 한 곳에 응집해 drift 방지. spec-reviewer가 신규 그리드 PR에서 체크리스트로 검증. |
| 2026-05-02 | DatePicker 표준 컴포넌트 도입 + 공휴일관리 그리드 baseline 전환 + native `<input type="date">` 전면 제거 | `apps/web/components/ui/DatePicker/**`, `apps/web/app/(app)/holidays/**`, `apps/web/components/grid/cells/EditableDateCell.tsx`, `.claude/skills/jarvis-architecture/SKILL.md`("표준 입력 컴포넌트" 섹션 신설), `CLAUDE.md` | 한국 IME 환경에서 native date input의 자릿수 자동 분할이 깨져 연도 칸에 6글자가 입력되는 버그 + 토/일/공휴일 시각 표시 요구. 11개 사용처 일괄 sweep. 향후 모든 달력 사용처에서 강제 |
| 2026-05-04 | **CI db:migrate 인프라 수정 — Option D (외부 사용자 흐름) 채택.** PR #103 이후 `packages/db/drizzle/` gitignore 정책에도 불구하고 PR #70/#78/#82/#108/#110 등이 `git add -f`로 0062-0070 SQL + `_journal.json`(idx 0-70 entries)을 강제 커밋 → CI `pnpm db:migrate`가 0000 SQL 부재로 ENOENT, schema-drift/unit-and-integration/e2e-wiki 모두 실패. 해결: ① 강제 추가 19개 파일 `git rm --cached` (drizzle/00{62-70}*.sql + meta/00{62-70}_snapshot.json + meta/_journal.json), ② 3개 워크플로우(schema-drift/test/e2e-wiki)에 "Generate baseline migration from schema" step 추가 → `rm -rf packages/db/drizzle && pnpm --filter @jarvis/db generate --name ci_baseline` (README §17.1 외부 사용자 흐름과 정합), ③ `.local/db/drizzle/`에 0000-0070 archive 복구 (71/71 SQL, 63/71 snapshot — 0037-0044/0046 8개는 git history에 한 번도 없음, 향후 generate에는 LATEST snapshot만 필요해 무영향), ④ `scripts/build-db-private-zip.mjs` 신설 (private zip 채널 §17.2 자동화 — `.local/db/drizzle/` + `packages/db/seed/`를 `packages\db\...` 경로로 zip해 서버 unzip 시 그대로 복원). | `.github/workflows/{schema-drift,test,e2e-wiki}.yml`, `packages/db/drizzle/` (untrack 19), `scripts/build-db-private-zip.mjs` (신설), `.local/db/drizzle/` (운영자 로컬 archive — git 무관) | PR #108-#111이 모두 같은 root cause로 fail. 기능 영향 없음 (schema TS가 SoT, 로컬/운영 DB는 그대로, gitignore는 디스크 파일 안 지움). 외부 GitHub 공개 정책(README §17) 유지 + zip 채널 정착으로 향후 운영 배포 표준화. |
| 2026-05-04 | **Legacy Oracle → Jarvis Postgres ETL 메타 전략 도입.** 3-layer hybrid: Layer 1 = TS bootstrap (`packages/db/seed/*.ts`), Layer 2 = Python→SQL ETL (`.local/legacy-ssms/postgres/<NN>_<module>/`), Layer 3 = `pnpm db:reset` runner (`.local/scripts/db-reset.sh`로 위임 — drop drizzle+public schema → migrate → seed conditional → ETL apply, lex sort). Pure INSERT + deterministic UUID(uuid5) → 멱등 wipe-and-reload. 외부 사용자(`.local/scripts/db-reset.sh` 부재) `pnpm db:reset` 자체가 fail — 사내 사용자만 실행 가능. 운영 zip 채널(`.local/scripts/build-db-private-zip.mjs`)에 ETL SQL + db-reset.sh 포함. 새 모듈은 `cp etl-template.example.py etl-<module>.py` 후 채우기 (helper는 `etl-template.py`). 본 PR은 메타 인프라(template + db:reset + zip 보강 + 문서)만 만들고, 각 모듈 ETL은 follow-up spec/plan/실행. P0 = 70_operations(maintenance/schedule/doc-numbers/faq) 4개. **운영 도구는 모두 `.local/` 하위 (gitignored, zip 채널)**: scripts·spec·plan·HANDOFF 모두 `.local/scripts/` 또는 `.local/docs/`. `packages/db/seed/sales-{plan-perf,stats-codes}.ts`는 `git rm --cached` 정책 정합. **Follow-up:** etl-contractor.py:221이 hardcoded `out_dir = base / "postgres" / "contractor"` — Task 4 verification에서 폴더를 `40_contractor`로 rename했지만 다음 regen 시 `contractor/` 다시 만듬. 단순 수정 별도 PR. | `package.json`(`db:reset` → `bash .local/scripts/db-reset.sh`), `README.md` §17 보강, `CLAUDE.md`, **(신설, gitignored)** `.local/scripts/db-reset.sh` + `.local/scripts/build-db-private-zip.mjs` + `.local/docs/2026-05-04-...md` + `.local/legacy-ssms/scripts/etl-template{,.example}.py` + `.local/legacy-ssms/postgres/40_contractor/` rename | 사용자 발언 "전체 데이터 oracle => postgre인데 새로운 스키마에 옮기는 대작업" + "DB 데이터 git 추적 금지". 단발성 X — 로컬에서 wipe-and-reload 반복 + 운영 동일 파일로 배포. 기존 자산(etl-contractor, sales/postgres, packages/db/seed/menus.ts) 깨지 않음. 운영 도구 자체도 외부 GitHub 노출 금지 → `.local/` 하위. spec/plan은 zip 채널의 `.local/docs/` 참조. |
| 2026-05-04 | **DB seed pipeline 부활 + Phase 2/3 ETL 실행 (메타 전략 후속 — P0 70_operations + 10_admin/user 완료).** Layer 1 (전부 gitignored, 디스크 only) `packages/db/seed/{dev,permissions,menu-permissions,companies-tsmt001,code-groups,parsers/parse-tsmt001,parsers/__tests__/parse-tsmt001.test}.ts` 부활/신설: PERMISSIONS const(47) + ROLE_PERMISSIONS(5 role) → permission/role/role_permission 130 mapping + menu_permission 99 mapping 시드. workspace 3 (jarvis + e2e + _system). dev admin user 1명 (employee_id=ADMIN, **scrypt** password hash — bcrypt 아닌 jarvis-auth 패턴, env override `DEV_ADMIN_PASSWORD`). TMST001 → company 366 + code_group 3 (C10100/C10005/C10002) Layer 1 예외로 유지(TS parser 활용). Layer 2 ETL (gitignored): `etl-admin-user.py` (TSYS305 → user 122 incl 26 contractor, 1 email dedup) + 70_operations 4 ETL (`etl-{maintenance,schedule,document-number,faq}.py` → maintenance_assignment 322 + schedule_event 7 + document_number 502 + faq_entry 13). Findings F2 + F3 해소. `40_contractor/01_user_contractor.sql` 폐기 (TSYS305 ETL로 통합 — `00_sabun_uuid_map.tsv` + `_skip_report.txt` 동시 삭제). `db-reset.sh` 보강 3건: ① psql PATH 미발견 시 `/c/Program Files/PostgreSQL/*/bin` fallback ② `packages/db/drizzle/_journal.json` 부재 시 `pnpm --filter @jarvis/db generate --name ci_baseline` 자동 실행 ③ `WORKSPACE_ID` 기본값 `7a995ab9-525c-415a-a8ea-34863fe002bb` (jarvis) — E2E는 명시 override. `etl-template.py`에 multi-line INSERT 파싱 추가 (TITLE/MEMO/QUESTION에 embedded `\n` 처리, single-line 호환). `pnpm db:reset` 16-table 멱등 검증 통과 (2회 연속 동일 count). `.local/docs/2026-05-04-legacy-etl-meta-strategy-design.md` §6.1/6.2/6.4/6.5/§10 갱신. **Follow-up (별도 PR):** sales/ Oracle REM 주석 정리 + 70_operations FK skip 보완 (legacy SABUN 부재 사용자 처리, 76+55 row). | **(commit)** `CLAUDE.md` 변경 이력만. **(gitignored, 디스크 only — 사내 zip 채널 배포)** `packages/db/seed/{dev,permissions,menu-permissions,companies-tsmt001,code-groups,parsers/parse-tsmt001}.ts` + `parsers/__tests__/parse-tsmt001.test.ts` · `packages/db/seed/menus.ts` (MENU_SEEDS export) · `.local/scripts/db-reset.sh` (3개 보강) · `.local/legacy-ssms/scripts/{etl-template(multi-line),etl-contractor(F2/F3),etl-admin-user(신설),etl-{maintenance,schedule,document-number,faq}(신설)}.py` · `.local/legacy-ssms/postgres/{10_admin/01_user,40_contractor/{02,03}_*(regen),70_operations/{01,02,03,04}_*(신설)}.sql` · `.local/docs/2026-05-04-legacy-etl-meta-strategy-design.md` (§6/§10 갱신) | docker DB가 partial state에 있었음 (permission 29/47, company 732 dup 380, organization/holiday 0, maintenance/schedule/doc-num/faq 테이블 부재). 폐기된 `.local/db/seed/` 부활 + 누락 시드(permission 18 + menu_permission 99) 추가. PERMISSIONS SoT(47개) ↔ DB drift 해소. **사용자 결정:** SABUN 전략 A · TMST001 366 Layer 1 유지 · workspace_id placeholder · TSMT100 SEQ→orderSeq · TSMT050 PROJECT_ID drop (502/502 NULL 확인) · 40_contractor user 폐기 · 데이터 보존 없음. **사용자 발언:** "지금 db seed만들면 이걸로 로컬테스트 쭉 계속하고 다되면 운영서버 db에 넣고, 앞으로 운영 서버db에 붙어서 직접 작업할 예정". 운영 zip 채널이 self-contained (psql PATH + drizzle baseline + WORKSPACE_ID 모두 자동 처리). |
| 2026-05-11 | **React/Next.js 보안 패치 — 12 CVE 일괄 대응.** `apps/web/package.json`: `next ^15.5.15 → ^15.5.16` (lock: 15.5.18), `react ^19.0.0 → ^19.2.6`, `react-dom ^19.0.0 → ^19.2.6`, `eslint-config-next ^15.5.15 → ^15.5.16`. React Team + Vercel이 2026-05 공동 공개한 보안 권고 — High 6건(RSC DoS · segment-prefetch middleware bypass · Cache Components 연결고갈 DoS · 동적 라우트 파라미터 미들웨어 우회 · WebSocket SSRF · Pages Router i18n bypass) + Moderate 4건(CSP nonce XSS · beforeInteractive XSS · Image Optimization DoS · RSC cache poisoning) + Low 2건. App Router only이라 Pages Router i18n CVE는 직접 미해당이나 패치 자체는 동일 버전에 포함. 검증 게이트: `pnpm --filter @jarvis/web type-check` (2회 통과) · `lint` (2회 — 기존 unused-var WARN만, ERROR 0) · `build` (전체 라우트 + 미들웨어 컴파일 성공) · `pnpm audit:rsc` (2회 — ERROR 0, WARN 181 기존 baseline 동일). breaking change 없음 (patch level only). 다른 패키지(apps/worker, packages/*)는 React/Next 의존성 없음 → 영향 없음. | `apps/web/package.json`, `pnpm-lock.yaml`, `README.md` §18, `CLAUDE.md` 변경 이력 | Cloudflare 보안 권고(2026-05) — WAF 차단 불가능한 High 6건(미들웨어 우회·SSRF·DoS) 포함 → 애플리케이션 코드 패치가 유일한 대응 수단. 패치 적용 미루면 서비스 거부·인가 우회·XSS·캐시 포이즈닝 노출. |
| 2026-05-12 | **Sensitivity row-level filter 전면 폐기 — RBAC + workspaceId 단일화.** 8 테이블(knowledge_page/knowledge_claim/wiki_page_index/notice/precedent_case/raw_source/graph_*/project/infra_system) sensitivity 컬럼 + sensitivity_enum drop · auth helper 14개 (canAccessKnowledgeSensitivity·buildWikiSensitivitySqlFilter·canViewWikiPage·canViewInternalNotice·canResolveProjectSecrets 등) 완전 제거 · Ask AI 4-tool agent `withSensitivityFilter` → `withWorkspaceRbacFilter`로 단순화 (시그니처 유지) · UI sensitivity 입력/표시/배지 일괄 제거 · A9 F1 notice canViewInternal fix revert · A8 B-P0-02 wiki ACL 게이팅 폐지 · Q1/Q2 자동 해소 (jarvis-db-patterns §4 §9.3 정책 폐기) · D5 SecretRef 권한 분기 제거 (PROJECT_READ면 모두 노출). | `packages/db/schema/{knowledge,wiki-page-index,notice,case,file,graph,project,system}.ts`, `packages/auth/{rbac,wiki-acl,index}.ts`, `packages/ai/agent/tools/**`, `apps/web/lib/queries/{notices,projects,infra-system,knowledge,dashboard-wiki-*}.ts`, `apps/web/app/(app)/{knowledge,wiki,notices,projects,infra,sales/*}/*`, `apps/web/messages/ko.json`, `apps/web/lib/server/wiki-sensitivity.ts` 삭제, `packages/db/drizzle/<NNNN>_sensitivity_drop.sql` (gitignored, 로컬 DB 적용 완료) | 사용자 결정: "RBAC만 하고 화면 자체를 안 보여주는 방식". row 단위 격리 진짜 필요한 도메인 없음 — 멀티 workspace 모델이 동등 격리 제공. helper drift 다수 + 모순 정책(Plan 5 vs §9.3) 일소. |
| 2026-05-12 | **그리드 표준 신규 룰 — `GridSearchForm.onResetGrid` required prop 도입 (A 패턴 전면 적용).** 조회 클릭 시 dirty/new/deleted 행을 사용자 확인 없이 즉시 폐기. required prop → 누락 시 TS 컴파일 에러로 차단. `DataGrid`는 `onGridReady` 콜백으로 `discardChanges` API 노출(GridContainer가 외부에서 트리거). 34 그리드 callsite 일괄 wiring (useGridState 직접 / DataGrid 캡슐화 / sub-component 3패턴). `jarvis-architecture` "그리드 표준 화면"에 필수 기능 7+1 → 7+1+1, PR 체크리스트 1항 추가. | `apps/web/components/grid/{GridSearchForm,DataGrid}.tsx`, 34 GridContainer/Grid 자식 컴포넌트, `.claude/skills/jarvis-architecture/SKILL.md` | 사용자 요구 "그리드 수정 상태에서 조회 누르면 그리드 초기 상태로 변경 — 입력/수정/삭제 이벤트 해놓은 상태 폐기". 모든 그리드 화면에서 일관 적용. 신규 그리드 추가 시 TS가 wiring 누락 차단. |
| 2026-05-12 | **하네스 경량화 점검** — `/harness` 5-스킬 audit 후속 정리. (1) sensitivity 폐기 후속 — db-patterns 반복 메시지 12군데 중 핵심 응집점 2곳(§정책, §4 격리)만 유지 + 나머지 cross-ref로 단축 / wiki-feature line 138(frontmatter sensitivity 신규 작성 금지) + line 205(cross-ref) 정리. (2) jarvis-architecture 657 lines → 518 lines (500 budget 안) — "그리드 표준 화면" 섹션 153 lines를 `references/grid-standard.md`로 분리해 progressive disclosure 적용 (그리드 작업 시에만 로드, 본문은 9개 하위섹션 인덱스 + 포인터). (3) CLAUDE.md 스킬 라인 통계 갱신 (31→54 스키마, 34→47 권한, "sensitivity"→"RBAC + workspaceId"). 5 스킬 + 0 에이전트 + superpowers 방법론 위임 구조 변경 없음. | `.claude/skills/jarvis-architecture/SKILL.md` (657→518), `.claude/skills/jarvis-architecture/references/grid-standard.md` (신설 153), `.claude/skills/jarvis-db-patterns/SKILL.md` (447→444), `.claude/skills/jarvis-wiki-feature/SKILL.md` (2줄), `CLAUDE.md` | 직전 커밋 8a72709(grid onResetGrid) + ba19a6a(sensitivity 폐기) 반영 확인 + 후속 drift 정리. 경량 3인 팀 기조 유지. progressive disclosure로 그리드 표준 컨텍스트는 그리드 작업 시에만 로드되어 비-그리드 작업에서 컨텍스트 절약. |
| 2026-05-16 | **RBAC 단순화 — 47권한·5역할 → 23권한·4역할 (-51% / -20%).** 변형 2-tier 패턴: 모든 도메인 `{read, admin}` + `admin:all` 마스터. 폐기 24권한 도메인 흡수 매핑 — knowledge/project/notice CRUD→admin, additional-dev→project, service-desk+month-report+maintenance-stats→maintenance, contractor→user, audit:read+files:write→admin:all, sales:all→sales:admin, *:write→*:admin. 5역할 → 4역할: ADMIN/MANAGER 유지(name 한글), DEVELOPER+HR+VIEWER → MEMBER(MEMBER=read+schedule:admin 10권한), 신규 YEAREND(jarvis 권한 0, 외부 yearend 사이트 식별 라벨). 한글 라벨 `ROLE_LABELS` 신규(`관리자/매니저/일반/연말정산`). owner check 패턴 도입(knowledge PUT/DELETE/review + schedule update/delete, ADMIN_ALL 우회). `/admin/roles` 신규 페이지(master/detail 그리드 표준, ADMIN_ALL 가드, role↔permission 매핑 UI). Ask AI RBAC bypass 플래그 `ASK_AI_RBAC_BYPASS=false`(기본, prod 안전 — true는 테스트 환경 전용 + 경고 주석). 운영 DB 10단계 트랜잭션 마이그레이션 (BEGIN→permission INSERT 23+DELETE 32+role INSERT 2+DELETE 3+user_role 재매핑+role_permission 재배치 54건+menu_permission CASE 매핑 75건+DO BLOCK 검증→COMMIT). 멱등(ON CONFLICT/DELETE+INSERT). 검증 통과: permissions=23, roles=4, role_permissions=54(23+21+10+0), user_role/menu_permission orphan=0. e2e `sidebar-rbac.spec.ts` 4역할(ADMIN/MEMBER/YEAREND)로 갱신 + `/admin/roles` 가드 케이스 추가. | `packages/shared/constants/permissions.ts`(SoT 전면 재작성 + ROLE_LABELS), `packages/shared/constants/permissions.test.ts`(재작성), `packages/auth/rbac.ts`(contractor 헬퍼 USER 권한 사용), `packages/auth/__tests__/rbac-contractor.test.ts`, `packages/ai/agent/ask-agent.ts`(RBAC_BYPASS flag), `apps/web/**` 39 파일(권한 호출처 일괄 rename), `apps/web/app/(app)/admin/roles/{page,actions}.ts` + `_components/{RolesPageClient,RoleGrid,RolePermissionGrid,useRolePermissionGridState}.tsx` (신규), `packages/shared/validation/admin/role.ts`(신규), `apps/web/app/api/knowledge/[pageId]/{route,review/route}.ts`(owner check), `apps/web/app/(app)/schedule/actions.ts`(owner check), `apps/web/app/api/knowledge/[pageId]/route.test.ts`+`apps/web/app/(app)/schedule/actions.test.ts`(신규 14 케이스), `apps/web/messages/ko.json`(Admin.Roles.* 36키), `apps/web/e2e/sidebar-rbac.spec.ts`(4역할 갱신 + /admin/roles 가드), `.env`+`.env.example`(ASK_AI_RBAC_BYPASS), `.local/legacy-ssms/postgres/10_admin/06_rbac_simplification.sql`(신설, 운영 DB 적용 완료), `.local/scripts/seed-menus.sql`(구권한 코드 4건 갱신 + ADMIN_ROLES menu_item 1건 추가), `docs/superpowers/plans/2026-05-16-rbac-simplification.md`(신설 plan 문서). | 사용자 요구 "UI에 47권한 다 보여서 너무 비대 — 단순화". 디스커션 7라운드 합의: ① 변형 2-tier(read/admin) + admin:all ② 4역할(관리자/매니저/일반/연말정산) ③ owner check는 knowledge/schedule 2도메인만(RBAC 밖 도메인 로직) ④ Ask AI bypass 테스트 단계 ⑤ `/admin/roles` UI 신설(역할 권한 매핑 변경 가능). 사용자 발언 "RBAC 흐려지지 않냐"에 read/admin이 표준 RBAC 패턴(AWS IAM ReadOnlyAccess + AdministratorAccess 동일)임을 확인. owner check 도메인 축소(7→2)는 "권한 가진 사람=다 가능, 권한 부여로 통제" 운영 정책 채택. type-check 20/20 통과(monorepo), lint ERROR 0, 신규 unit test 14 케이스 통과, 운영 DB 트랜잭션 검증 통과(자동 ROLLBACK 안전망). |
| 2026-05-16 | **RBAC docs drift 일괄 갱신 (follow-up).** 5 하네스 스킬 + AGENTS.md + CONTRIBUTING.md + WIKI-AGENTS.md + README.md §18에 RBAC simplification 반영. (1) `jarvis-architecture` §3 권한 — PERMISSIONS 47→23 + 5역할→4역할 + 한글 라벨 + owner check 패턴 + 영향도 체크리스트 갱신 + 메뉴 권한 매트릭스 DEVELOPER/HR/VIEWER 열 → MEMBER/YEAREND 열 + maintenance 흡수(service-desk+month-report) + user 흡수(contractor) + sales:all→sales:read/admin 반영. (2) `jarvis-db-patterns` §2 — 권한 전수 표 47→23 도메인 그룹 재구성 + ROLE_PERMISSIONS 매핑 5역할→4역할(개수 23/21/10/0 명시) + §2.2 owner check 패턴 신설(knowledge/schedule 적용 도메인 + 코드 패턴) + 폐기 권한 명시. (3) `jarvis-feature` 영향도 라인 47상수·5역할→23상수·4역할. (4) `jarvis-wiki-feature` §7.3 권한 상수 KNOWLEDGE_REVIEW 폐기 반영 + §7.6 review-queue `KNOWLEDGE_REVIEW`→`KNOWLEDGE_ADMIN`. (5) AGENTS.md 도메인 스킬 표 카운트 + RBAC 본문 PERMISSIONS 47→23·5역할→4역할 + ROLE_LABELS 추가 + 변경 이력 entry. (6) CONTRIBUTING.md 도메인 스킬 표 카운트 갱신. (7) WIKI-AGENTS.md 역할×위키 접근 매트릭스 4역할로 재작성 + 한글 라벨. (8) README.md §18 + AGENTS.md 변경 이력 + CLAUDE.md 변경 이력 entry 추가. (9) `.env.example` `ASK_AI_RBAC_BYPASS` 기본 false + prod 경고 주석. (10) `/admin/roles/actions.ts` 시스템 role 권한 매핑 불변 가드. (11) `packages/shared/validation/admin/role.ts` update patch에 id/code/isSystem 차단. (12) knowledge owner check에 `knowledge_page_owner` 테이블 함께 검사 (legacy `created_by=NULL` 페이지 차단 fix). (13) 06_rbac_simplification.sql validation 하드코딩 UUID → `:'WORKSPACE_ID'` 변수. (14) e2e helpers/auth.ts + dev-accounts.ts VIEWER/DEVELOPER → MEMBER. (15) RBAC 관련 unit test 10건 갱신(dev-accounts/admin-users/review-queue/knowledge-route owner mock). 추가로 graphify build route literal `'graph:build'` → `PERMISSIONS.GRAPH_ADMIN` (P1 누락 fix). admin/users role enum DEVELOPER/HR/VIEWER → MEMBER/YEAREND + 기본 MEMBER. | `.claude/skills/jarvis-architecture/SKILL.md` (3 spots), `.claude/skills/jarvis-db-patterns/SKILL.md` (3 spots), `.claude/skills/jarvis-feature/SKILL.md` (2 spots), `.claude/skills/jarvis-wiki-feature/SKILL.md` (2 spots), `AGENTS.md` (3 spots + 변경 이력 entry), `CONTRIBUTING.md`, `WIKI-AGENTS.md` (역할 매트릭스 재작성), `README.md` §18 entry, `CLAUDE.md` 본문 line 13 + 이 entry, `.env.example` (bypass 기본 false), `apps/web/app/(app)/admin/roles/actions.ts` (시스템 role guard), `packages/shared/validation/admin/role.ts` (roleUpdatePatch 분리), `apps/web/app/api/knowledge/[pageId]/route.ts` + `review/route.ts` (knowledge_page_owner 검사), `.local/legacy-ssms/postgres/10_admin/06_rbac_simplification.sql` (validation UUID 변수화), `apps/web/e2e/helpers/auth.ts`, `apps/web/lib/auth/dev-accounts.ts`, 6 test 파일 갱신 (dev-accounts/admin-users/review-queue/knowledge-route/graphify-route/admin-menus 라벨), `apps/web/app/api/graphify/build/route.ts` (literal→PERMISSIONS), `apps/web/app/api/admin/users/route.ts` (role enum). | codex review 8 finding(P1 4건 + P2 4건) 전부 fix. 사용자 요구 "README나 하네스 관련 문서들 이번 건과 관련된거 싹다 찾아서 수정해야 함". 코드 변경 후 문서 drift 즉시 동기화하여 다음 세션·다른 에이전트(Codex CLI 등)가 stale 정보 읽지 않도록. type-check 20/20 통과, lint ERROR 0, RBAC unit test 10/10 갱신·통과(전체 873 pass / 8 pre-existing fail). 한글 라벨 `ROLE_LABELS`는 외부 yearend 사이트 식별("연말정산")까지 포함. |
| 2026-05-16 | **디자인 시스템 도입 — Q5=B swap + brand-primary `color-mix()` 파생 + 5테마 picker + 사이드바 active 시각 + 다크테마 동일 적용.** mockup 2회 검증(`.local/design-preview/theme-collision-mockup.html`) 후 사용자 결정 11건 기반 단일 worktree. (1) **CSS 토큰 swap** `--bg-page: #fff → warm-50(#faf9f8)`, `--bg-surface: warm-50 → #fff` — 사용자 사진 1·2 회색 띠 일소. prototype alias(`--panel`/`--bg`/`--accent*`) + shadcn bridge(`--color-primary`/`--color-accent*`/`--color-ring`)도 SoT(`--brand-primary*`) 참조로 통일 → 테마 picker cascade 끊김 차단. (2) **brand-primary 파생** hover/bg/text를 `color-mix(in oklab, ...)`로 자동 (라이트 80/20·8/92, 다크 70/30·18/transparent). `--border-focus`도 `var(--brand-primary)` 참조. (3) **5테마** Notion Blue `#0075de` / Indigo `#5e6ad2` / Teal `#2a9d99` / Forest `#0f6e3a`(`#1f883d`에서 보정 — Done chip과 명도 분리) / Graphite `#171717` (다크 `#f5f5f5` 반전, bg와 충돌 방지). **Sunset `#dd5b00` 제외** — `--color-orange` Warn 상태색과 완전 동일 hex(의미 충돌). `data-theme-color={blue,indigo,teal,forest,graphite}` 10블록(light + dark). (4) **사이드바 active 시각** expanded는 `var(--line2)` warm pill → `var(--brand-primary-bg)` 8% 틴트 pill + `var(--brand-primary-text)` 글자색 + 좌측 3px `var(--brand-primary)` indicator(rail와 패턴 통일). (5) **picker UI** `uiPrefs.ts`에 `useThemeColor`/`setThemeColor` + `THEME_COLOR_IDS as const`(typed whitelist) + `UI_PREFS_BOOTSTRAP` 확장(FOUC 방지 inline script). `ThemeColorPicker.tsx` 5 swatch radio group(aria-checked/aria-label/키보드). `UserMenu.tsx` "테마 설정 [준비 중]" → 활성 submenu(aria-haspopup/expanded, ChevronDown rotation, dropdown 닫힘 시 picker reset). (6) **i18n** `Theme.picker.{title,aria}` + `Theme.colors.{blue,indigo,teal,forest,graphite}` + `Common.themeColor`/`themeColorRegion` (ko + en, 9×2 = 18 키). (7) **TDD 검증** uiPrefs 6 vitest + ThemeColorPicker 5 vitest + theme-picker 5 playwright(2회 연속 5/5 PASS). type-check×2, lint×2(WARN baseline), audit:rsc(ERROR 0), build PASS. **주의**: 같은 날 main의 RBAC simplification(47→23권한·5→4역할)과 별개 PR. 본 entry의 권한 카운트(47/5) 언급은 도입 당시 기준이며, 현재 정답은 위 RBAC entry 참조. | `apps/web/app/globals.css`, `apps/web/components/layout/{Sidebar,UserMenu,uiPrefs,ThemeColorPicker,ThemeColorPicker.test,uiPrefs.test}.tsx/ts`, `apps/web/messages/{ko,en}.json`, `apps/web/e2e/theme-picker.spec.ts`, plan `docs/superpowers/plans/2026-05-16-design-system-adoption.md` | 사용자 결정 11건(brainstorming 단계에서 mockup 2회 + 토론 5라운드): Q5=B(swap) · 5테마 확정 · Sunset 제외 · Forest hex 보정 · color-mix 파생 · 사이드바 active 변경 · 다크 적용 · UserMenu submenu(option B) · status chip 형태 유지 · shadow-soft 유지 · border 0.10 유지. Sunset 제외 사유: hex 보정 불가(빨강쪽 → Danger 충돌, 노랑쪽 → Warn 그대로 — 오렌지 hue 함정). Forest 보정 사유: `#1f883d` Done chip(`#1aae39`)과 거의 동일 → `#0f6e3a` 어두운 emerald로 명도 분리. Graphite 다크 반전 사유: `#171717` ≈ 다크 bg(`#191918`) 보이지 않음. 단일 PR(worktree `design-system-adoption`). 따로 PR 분할 비용보다 사용자 검증 1라운드 + 단일 commit-set이 합리적. |
| 2026-05-16 | **디자인 시스템 v4 후속 fix — `--bg-canvas` 토큰 도입.** ultrareview 5 findings 발견: 57 파일 98 occurrence가 `bg-(--bg-page)` 사용 (shadcn card body, dialog, popover, dropdown-menu, sheet, select, toast, tabs, input, textarea, checkbox, radio-group, button outline 등 모두 elevated chrome). 직전 commit의 swap이 elevated 의미를 invert → 카드 body warm-50 / 카드 header #fff inverted. 해결: swap revert + 신규 `--bg-canvas` 토큰(warm-50, page only) 도입. 57 파일 무수정. 동시 fix: graphite L-extreme hover 평탄화 → 라이트 `#3a3a3a` / 다크 `#d4d4d4` 명시 override · `useThemeColor` lazy init(blue flash 제거) · ThemeColorPicker 다크 graphite swatch `#f5f5f5` 표시(useTheme 분기) · UserMenu `aria-haspopup`/`role="region"` ARIA 오용 제거 · ThemeColorPicker APG radiogroup roving tabindex + ArrowLeft/Right/Up/Down/Home/End 키보드. `themeColorRegion` i18n 키 제거(unused). | `apps/web/app/globals.css` (canvas + 다크 3단계 계층), `apps/web/components/layout/{uiPrefs,UserMenu,ThemeColorPicker}.tsx`, `apps/web/messages/{ko,en}.json`, `docs/design-system.md`(v4 토큰 모델 명시) | ultrareview commit `353c8251`. 직전 swap이 user 사진 회색 띠는 일소했으나 카드 body↔header 색 invert 부작용. canvas 도입으로 시각은 동일(페이지=warm-50, chrome+card=#fff) + 57 파일 의미 보존. APG radiogroup 패턴은 spec docs와 정합. |
| 2026-05-16 | **디자인 v5 — bg 토큰 통합 + chrome/카드 순백 일괄 적용.** v4(canvas warm-50 페이지)에서 사용자 피드백("warm 회색 부분 남아있는게 많음")으로 v5 단순화. 디자인 킷 chat 최종 결정(페이지+카드+chrome 모두 흰색, warm-50은 미세 tint만) 코드베이스 전면 적용. (1) globals.css `--bg-canvas` 토큰 제거 + body bg → `var(--bg-page)` (라이트 #fff, 다크 #191918). 다크 계층 단순화(--bg-page #191918, --bg-surface #242322). (2) `bg-(--bg-surface)` 52 파일 97 occurrence 일괄 sweep → `bg-(--bg-page)` (sed substring 매칭, opacity suffix 보존). (3) 명시 4 패턴 중 2건 복원 — DataGrid `<thead>` head row + WikiWidget TabBtn inactive hover. (quiz option은 `--bg-hover` 별도 토큰 사용 중 / wiki row는 background hover 자체가 없음.) (4) Topbar TabBar 시각 — chrome-tab 3변 border + rounded corner + overlap → 단순 underline (`borderBottom: 2px var(--brand-primary)` active) + `var(--brand-primary-bg)` 8% 틴트 배경 + `var(--brand-primary-text)` 글자. hover `var(--bg-surface)`. (5) Radius audit — `rounded-2xl/3xl` 6 occurrence 검토: UserMenu dropdown + AccessEntryForm secret → `rounded-xl`. login/reset-password T1 hero + AskPanel 채팅 버블 특수 형태 유지. (6) 하드코드 hex 6 occurrence (PDF template + dev a11y) — UI cascade 무관, skip. 검증: type-check + lint + build + vitest 11/11 + audit:rsc(ERROR 0). | `apps/web/app/globals.css` (canvas 제거, 다크 계층), `apps/web/**/*.tsx/ts` (52 파일 bg-surface sweep + 4 파일 복원/Topbar/UserMenu/AccessEntryForm), `docs/design-system.md` v4→v5, `CHANGELOG.md`, `AGENTS.md`, `CLAUDE.md` 이 entry | 사용자 발언: "이렇게 밝은 회색 warm 부분 남아있는게 많음" + ui_kits/web/styles.css 톤 매칭 요구(페이지 + 카드 같은 #fff, border+shadow로만 구분). v4 canvas 모델은 v3 swap → ultrareview fix → v5 단순화의 진화 경로. 별도 worktree 없이 main 직접 작업(6 commits, 단일 push). 시각 결과: 회색 띠 완전 제거 + DataGrid head/wiki tab hover 등 의도된 tint만 미세 잔존. |
