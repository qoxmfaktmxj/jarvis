# Data Sources

- 현재 로컬 데모 데이터는 `samples/**` synthetic fixture만 사용합니다.
- 이메일/URL은 `example.invalid`만 허용합니다.
- 실제 운영 데이터나 copied historical corpora는 포함하지 않습니다.
- 향후 공식 provider ingest는 별도 승인 후 `data:sync` 경로로만 연결합니다.
