/**
 * packages/ai/page-first/index.ts
 *
 * Phase-W2 T2 — page-first navigation sub-modules re-export.
 *
 * Pipeline sub-modules (shortlist / expand / read-pages / synthesize)은
 * ask.ts 및 tutor.ts에서 직접 import.
 */

export { PAGE_FIRST_PROMPT_VERSION, PAGE_FIRST_SYNTH_OP } from "./synthesize.js";
