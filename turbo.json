{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["package.json", "pnpm-workspace.yaml", "tsconfig.json", ".gitattributes"],
  "globalEnv": [
    "DATABASE_URL",
    "TEST_DATABASE_URL",
    "SESSION_SECRET",
    "WIKI_REPO_ROOT",
    "MINIO_ENDPOINT",
    "MINIO_ACCESS_KEY",
    "MINIO_SECRET_KEY",
    "MINIO_BUCKET",
    "LLM_GATEWAY_URL",
    "LLM_GATEWAY_KEY",
    "ASK_AI_MODEL",
    "INGEST_AI_MODEL",
    "ASK_DAILY_BUDGET_USD",
    "NODE_EXTRA_CA_CERTS"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "type-check": {
      "dependsOn": ["^type-check"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:integration": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "cache": false,
      "outputs": ["playwright-report/**", "test-results/**"]
    }
  }
}
